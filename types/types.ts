interface CommentProps {
  name: string;
  date: string;
  comment: string;
}

interface PostProps {
  name: string;
  date:string;
  imageUrl: string;
}

interface InteractionBarProps {
  numLikes: number;
}